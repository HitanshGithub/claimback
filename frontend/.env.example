# Copy to .env.local and adjust.

# Base URL of a deployed ClaimBack API (no trailing slash). Leave empty to use the
# Vite dev proxy (/api -> http://127.0.0.1:8000) or a same-origin deployment.
VITE_API_BASE=

# Set to 1 to serve fixtures from src/mocks instead of the network.
VITE_MOCK=0
